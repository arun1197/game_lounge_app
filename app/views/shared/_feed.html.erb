<% if @feed_items.any? %>
  <ol class="posts">
    <%= render @feed_items %> 
    <!-- ^^ @feed_items each item of Post class so it knows to call -->
    <!-- the post partial (_post.html.erb) -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>